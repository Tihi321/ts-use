export enum IFrameStates {
  FALSE = 0,
  TRUE = 1
}

export enum PlayerState {
  ENDED = 0,
  PLAYING = 1,
  PAUSED = 2
}
export enum EventTypes {
  READY = "ready",
  STATE_CHANGE = "stateChange"
}
